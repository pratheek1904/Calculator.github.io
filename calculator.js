function add(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first+second;
    myform.result.value=result;
}
function Sub(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first-second;
    myform.result.value=result;
}

function Mul(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first*second;
    myform.result.value=result;
}

function Div(){
    first=parseInt(myform.first.value);
    second=parseInt(myform.second.value);
    result=first/second;
    myform.result.value=result;
}